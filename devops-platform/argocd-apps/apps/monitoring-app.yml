apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring-management
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/skncvo/infra-repo.git
    targetRevision: main
    path: devops-platform/platform/monitoring
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
